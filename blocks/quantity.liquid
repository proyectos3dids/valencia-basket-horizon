

{% unless block.settings.product.metafields.custom.extra == true %}
  {% render 'quantity-selector', product: block.settings.product %}
{% endunless %}

{% schema %}
{
  "name": "t:names.quantity",
  "tag": null,
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "t:settings.product"
    }
  ]
}
{% endschema %}
